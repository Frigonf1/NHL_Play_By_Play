services:
  app:
    build: .
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    depends_on:
      - redis
    ports:
      - "8888:8888"  # For Jupyter

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  redis_data:
